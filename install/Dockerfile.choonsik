FROM billion-scale-benchmark

RUN apt-get update && apt-get install -y libopenblas-base libopenblas-dev libpython3-dev python3-dev libgoogle-perftools-dev
RUN pip3 install cython

COPY ./choonsik/big-ann.git big-ann.git
RUN cd big-ann.git && python3 setup.py clean --all install
RUN python3 -c 'from choonsik import BigAnnIndex'
